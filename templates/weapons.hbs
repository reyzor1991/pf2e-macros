<div class="scrollable">
    <div class="weapon-row flexrow">
        <label>Name of macro</label>
        <label>Slug/Id/Name of weapon</label>
    </div>
    {{#each weapons}}
        <div class="weapon-row flexrow">
            <label>{{this.label}}</label>
            <input type="text" name="{{this.id}}" value="{{this.value}}"/>
        </div>
    {{/each}}
</div>
